{
  "name": "exam-prep-lite",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "dev:local": "npm run local:catalog && VITE_CATALOG_FILE=./catalog.local.json EXAM_PREP_LOCAL=1 vite --host 127.0.0.1",
    "catalog": "node scripts/snapshot-materials.mjs",
    "local:import": "node scripts/import-local-cambridge.mjs",
    "local:catalog": "node scripts/snapshot-local-materials.mjs",
    "check:local": "npm run local:catalog && node scripts/check-local-library.mjs && npm run test && npm run build",
    "check:sources": "node scripts/check-sources.mjs --live",
    "validate:sources": "node scripts/check-sources.mjs",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0",
    "test": "node --test tests/*.test.mjs",
    "check": "npm run validate:sources && npm run test && npm run build"
  },
  "devDependencies": {
    "vite": "^8.2.2"
  },
  "dependencies": {
    "pdfjs-dist": "^6.3.289"
  }
}
